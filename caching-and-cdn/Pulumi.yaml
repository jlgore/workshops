name: caching-and-cdn
runtime:
  name: nodejs
  options:
    packagemanager: npm
description: A minimal AWS TypeScript Pulumi program deploying a redis cluster and a cloudfront backed api gateway.
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
